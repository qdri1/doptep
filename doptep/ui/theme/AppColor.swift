import SwiftUI

struct AppColor {
    static let primary = Color(hex: "#1E7F2F")
    static let onPrimary = Color(hex: "#FFFFFF")
    static let primaryContainer = Color(hex: "#A6DAB5")
    static let onPrimaryContainer = Color(hex: "#003917")
    static let inversePrimary = Color(hex: "#61B95E")

    static let secondary = Color(hex: "#1C1C1C")
    static let onSecondary = Color(hex: "#FFFFFF")
    static let secondaryContainer = Color(hex: "#CCCCCC")
    static let onSecondaryContainer = Color(hex: "#121212")

    static let tertiary = Color(hex: "#005CB2")
    static let onTertiary = Color(hex: "#FFFFFF")
    static let tertiaryContainer = Color(hex: "#B3D4FF")
    static let onTertiaryContainer = Color(hex: "#00254F")

    static let background = Color(hex: "#F2F2F2")
    static let onBackground = Color(hex: "#1A1A1A")

    static let surface = Color(hex: "#FFFFFF")
    static let onSurface = Color(hex: "#1A1A1A")
    static let surfaceVariant = Color(hex: "#E1E1E1")
    static let onSurfaceVariant = Color(hex: "#3C3C3C")
    static let inverseSurface = Color(hex: "#1A1A1A")

    static let error = Color(hex: "#D32F2F")
    static let onError = Color(hex: "#FFFFFF")
    static let errorContainer = Color(hex: "#FFCDD2")
    static let onErrorContainer = Color(hex: "#5C0000")

    static let outline = Color(hex: "#B0B0B0")
    static let outlineVariant = Color(hex: "#E5E5E5")
}
